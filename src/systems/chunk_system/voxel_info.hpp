#pragma once

namespace aiko
{
    namespace VoxelInfo
    {
        // TODO : Temporal format for this, this will be just temporally to have something working, will improve this in latest stages

        enum class VoxelType { AIR, SOLID, LIQUID };

        enum class VoxelId { BEDROCK, DIRT, GRASS };

    }

}